# Drizzle Migrations

Auto-generated SQL migration files live here after running `npm run db:generate`.

To apply migrations to your database:
```bash
npm run db:migrate
```

Or push the schema directly (useful for dev):
```bash
npm run db:push
```
